# reto

crear nuevo paquete:
    npm init

escribir el archivo para el punto de entrada

entry point: (index.js)server.js

Crear nuevo archivo 
    touch server.js

instalar paquetes 
    npm install express

compilar para verificar
    node server.js